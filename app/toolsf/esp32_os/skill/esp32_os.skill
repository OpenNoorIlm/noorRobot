# ESP32-OS bridge

Use the ESP32-OS HTTP API on port 8083. Eye expressions use `/eyes?q=Happy` or `/eyes?q=Happy,5,-2`; movement uses `/forward`, `/backward`, `/left`, `/right`, and `/stop`. The bridge communicates with an already-flashed ESP32 and never starts firmware.
